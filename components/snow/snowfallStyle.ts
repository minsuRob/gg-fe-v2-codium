import { StyleSheet } from "react-native";

export const SnowfallStyle = StyleSheet.create({
  container: {
    margin: 0,
    top: -200,
    left: 0,
    height: "70%",
    width: "100%",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    position: "absolute",
    backgroundColor: "transparent",
  },
});
